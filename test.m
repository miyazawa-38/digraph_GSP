%% MATLABの関数がどのような挙動をするのかを確かめるファイル

% A = [0,3,1;
%      1,0,7;
%      5,2,0];
% 
% x_send = sum(A,1)

a = [0;-3;1];
a = abs(a(:))

% g = exp(1 * a)

% a = 3 + 4i;
% abs(a)
% error('plotSignal: x and y must have the same number of elements');

